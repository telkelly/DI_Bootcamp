const greeting = (name) => console.log(`Hello,${name}! welcome to Node JS`);
const sayHello = (name) => console.log(`Hello,${name}! welcome to Node JS`);


module.exports = {
    greeting,
    sayHello
}